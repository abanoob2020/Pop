# Runtime deploy logs (created by deploy.sh)
logs/

# Uploaded files are runtime data — ignore contents but keep the hardening .htaccess.
dashboard/uploads/*
!dashboard/uploads/.htaccess

# Database backup artifacts (created by backup.sh)
backups/
